@page
@model LockoutModel
@{
    ViewData["Title"] = "Locked out";
}

<div class="text-center py-5">
    <h2 class="ds-title mb-2">Locked out</h2>
    <p class="ds-body mb-0" style="color: var(--ds-muted-foreground);">This account has been locked out due to too many failed login attempts. Try again later.</p>
</div>
